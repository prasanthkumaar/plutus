{
  "name": "plutus",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @plutus/mcp-server build",
    "dev": "pnpm --filter @plutus/mcp-server dev",
    "test": "pnpm --filter @plutus/mcp-server test",
    "type-check": "pnpm --filter @plutus/mcp-server type-check",
    "verify:mcp": "pnpm --filter @plutus/mcp-server verify:mcp"
  }
}
