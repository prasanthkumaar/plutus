{
  "name": "sh",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @sh/mcp-server build",
    "dev": "pnpm --filter @sh/mcp-server dev",
    "dev:1password": "op run --env-file=.env.schema -- pnpm dev",
    "test": "pnpm --filter @sh/mcp-server test",
    "type-check": "pnpm --filter @sh/mcp-server type-check"
  }
}
